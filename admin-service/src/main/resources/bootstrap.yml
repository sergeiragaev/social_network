spring:
  application:
    name: admin-console
  profiles:
    active: local
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8761}
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true

springdoc:
  api-docs:
    path: /api/v1/admin-console/statistic/v3/api-docs

# could be used for debugging
#logging:
#  level:
#    org:
#      springframework: TRACE

